pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

        //1
        maven { url "https://jitpack.io" }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //1
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "LableView"
include ':app'
